/**
 * Fichier : frontend/src/pages/sla/SlaPage.tsx
 *
 * Page placeholder pour le dashboard SLA.
 *
 * Rôle (à venir) :
 * ----------------
 * - présenter les indicateurs de niveau de service (SLA) ;
 * - fournir des vues synthétiques (KPI, courbes, histogrammes).
 *
 * Pour l'instant, la page est volontairement minimale.
 */
import React from "react";

const SlaPage: React.FC = () => {
  return (
    <section>
      <h1>SLA</h1>
      <p>Dashboard SLA – contenu à venir.</p>
    </section>
  );
};

export default SlaPage;
