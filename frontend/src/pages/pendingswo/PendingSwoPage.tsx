/**
 * Fichier : frontend/src/pages/pendingswo/PendingSwoPage.tsx
 *
 * Page placeholder pour le dashboard "Pending SWO".
 *
 * Rôle (à venir) :
 * ----------------
 * - suivi des SWO en attente (backlog) ;
 * - priorisation, ancienneté, impact.
 */
import React from "react";

const PendingSwoPage: React.FC = () => {
  return (
    <section>
      <h1>Pending SWO</h1>
      <p>Dashboard Pending SWO – contenu à venir.</p>
    </section>
  );
};

export default PendingSwoPage;
